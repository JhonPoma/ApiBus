package com.example.api_bus.repository;
import com.example.api_bus.model.Bus;
import org.springframework.data.jpa.repository.JpaRepository;
public interface BusRepository extends JpaRepository<Bus,Long>{
}
